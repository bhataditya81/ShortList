{
  "name": "shortlist",
  "private": true,
  "description": "Repo-aware tool shortlist + labeled Featured. Conversion cashback only where TOS allows. Not wait-state ads.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm run build -w @shortlist/catalog && npm run build -w @shortlist/ranker && npm run build -w @shortlist/data-store",
    "dev": "npm run build:packages && npm run dev -w @shortlist/web",
    "dev:web": "npm run dev -w @shortlist/web",
    "dev:mcp": "npm run start -w @shortlist/mcp",
    "build": "npm run build -w @shortlist/catalog && npm run build -w @shortlist/ranker && npm run build -w @shortlist/data-store && npm run build -w @shortlist/mcp && npm run build -w @shortlist/web",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
